#include<stdio.h>
int Saisie_V( int  V[10])
{
    int v[10];
    int i ;
    for (i=1 ; i<10; i++)
    {
        printf("v[%d]=",i) ;
        scanf("%d",&v[i]);

}

int Min_Max (int V[10])
{
int i ;
int max, min ;
min = V[i];
max= V[1];
for (i=0 ; i<10 ; i++)
{

    if (V[i]>max){
        max = V[i];
    }
     if (V[i]<min)

        min =v[i];
    }

            }
}
printf("%d,%d",max ,min);
}



void occuence( int v[10], int xo )
{

    int occ, i ;

occ= à ;
for (i=0 ;i<10 ; i++)
{

    if (v[i]=0)
        occ = occ+1 ;

}
}
